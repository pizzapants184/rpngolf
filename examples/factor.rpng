# by pizzapants184
# var x is the number we are factoring
# var l is the list of factors
# var o is a, x%a
# var i is the routine to 
#  while stack[-1] | x:
#   l.append(stack[stack[-1])
#   x /= stack[-1]
# var w is while(call var o):
#  call var i
tz
0lLvl
tbvx # input number and store in x
(puxsg)vo
#((pulslavlpuxsfvx)TBuoy)vi
#(WiWorAo)vw
((pulslavlpuxsfvx)WorAo)vw
Ww2ux1arD
ul
